x-db-url: &db-url
  DATABASE_URL: postgresql://${POSTGRES_USER:-trustcore}:${POSTGRES_PASSWORD:-changeme}@postgres:5432/${POSTGRES_DB:-trustcore_memory}

services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: trustcore-postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-trustcore}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-changeme}
      POSTGRES_DB: ${POSTGRES_DB:-trustcore_memory}
    ports:
      - "${DATABASE_PORT:-5432}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-trustcore}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  ollama-gpu1:
    image: ollama/ollama:latest
    container_name: trustcore-ollama-gpu1
    restart: unless-stopped
    ports:
      - "11434:11434"
    volumes:
      - C:/Users/Admin/.ollama:/root/.ollama
    environment:
      OLLAMA_HOST: 0.0.0.0:11434
      OLLAMA_MAX_LOADED_MODELS: "1"
      OLLAMA_KEEP_ALIVE: "-1"
      NVIDIA_VISIBLE_DEVICES: "1"
    networks:
      - default
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              device_ids: ['1']
              capabilities: [gpu]

  ollama-gpu0:
    image: ollama/ollama:latest
    container_name: trustcore-ollama-gpu0
    restart: unless-stopped
    ports:
      - "11435:11434"
    volumes:
      - C:/Users/Admin/.ollama:/root/.ollama
    environment:
      OLLAMA_HOST: 0.0.0.0:11434
      OLLAMA_MAX_LOADED_MODELS: "8"
      OLLAMA_KEEP_ALIVE: "0"
      NVIDIA_VISIBLE_DEVICES: "0"
    networks:
      - default
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              device_ids: ['0']
              capabilities: [gpu]

  alex:
    build:
      context: .
      dockerfile: Dockerfile.agent
      args:
        AGENT_MODE: alex
    container_name: trustcore-alex
    command: ["node", "--loader", "ts-node/esm", "src/index.ts", "alex"]
    env_file: .env
    environment:
      <<: *db-url
      OLLAMA_HOST: http://ollama-gpu1:11434
      OLLAMA_HOST_GPU0: http://ollama-gpu0:11434
      LLM_MODEL: "qwen3.5:35b-a3b"
      EVAL_POLL_TIMEOUT_MS: "300000"
    depends_on:
      postgres:
        condition: service_healthy
      ollama-gpu1:
        condition: service_started
    restart: unless-stopped

  research:
    build:
      context: .
      dockerfile: Dockerfile.agent
      args:
        AGENT_MODE: research
    container_name: trustcore-research
    command: ["node", "--loader", "ts-node/esm", "src/index.ts", "research"]
    env_file: .env
    environment:
      <<: *db-url
      OLLAMA_HOST: http://ollama-gpu0:11434
      LLM_MODEL: "qwen3.5:9b"
    depends_on:
      postgres:
        condition: service_healthy
      ollama-gpu0:
        condition: service_started
    restart: unless-stopped

  mcp:
    build:
      context: .
      dockerfile: Dockerfile.agent
    container_name: trustcore-mcp
    command: ["node", "--loader", "ts-node/esm", "src/index.ts"]
    env_file: .env
    environment:
      <<: *db-url
      OLLAMA_HOST: http://ollama-gpu1:11434
      OLLAMA_HOST_GPU0: http://ollama-gpu0:11434
    ports:
      - "3001:3001"
    depends_on:
      postgres:
        condition: service_healthy
      ollama-gpu1:
        condition: service_started
    restart: "no"

  email-writer:
    build:
      context: .
      dockerfile: Dockerfile.agent
      args:
        AGENT_MODE: email-writer
    container_name: trustcore-email-writer
    command: ["node", "--loader", "ts-node/esm", "src/index.ts", "email-writer"]
    env_file: .env
    environment:
      <<: *db-url
      OLLAMA_HOST: http://ollama-gpu0:11434
      LLM_MODEL: "qwen3.5:9b"
    depends_on:
      postgres:
        condition: service_healthy
      ollama-gpu0:
        condition: service_started
    restart: unless-stopped

  resource-manager:
    build:
      context: .
      dockerfile: Dockerfile.agent
    container_name: trustcore-resource-manager
    command: ["node", "--loader", "ts-node/esm", "src/resource-manager/standalone.ts"]
    env_file: .env
    environment:
      <<: *db-url
      OLLAMA_HOST: http://ollama-gpu1:11434
      OLLAMA_HOST_GPU0: http://ollama-gpu0:11434
    depends_on:
      postgres:
        condition: service_healthy
      ollama-gpu1:
        condition: service_started
    # nvidia-smi requires host GPU access; if unavailable the service uses mock data
    restart: unless-stopped

  api:
    build:
      context: .
      dockerfile: Dockerfile.agent
    container_name: trustcore-api
    command: ["node", "--loader", "ts-node/esm", "src/index.ts", "api"]
    env_file: .env
    environment:
      <<: *db-url
      OLLAMA_HOST: http://ollama-gpu1:11434
      OLLAMA_HOST_GPU0: http://ollama-gpu0:11434
      API_PORT: "3003"
    ports:
      - "3003:3003"
    depends_on:
      postgres:
        condition: service_healthy
      ollama-gpu1:
        condition: service_started
    restart: unless-stopped

  dashboard:
    build:
      context: .
      dockerfile: Dockerfile.dashboard
      args:
        NEXT_PUBLIC_API_URL: http://localhost:3003
    container_name: trustcore-dashboard
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:3003
    ports:
      - "3004:3000"
    depends_on:
      - api
    restart: unless-stopped

volumes:
  pgdata:

networks:
  default:
    name: trustcore-engine_default
